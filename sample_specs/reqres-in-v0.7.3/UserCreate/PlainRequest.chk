version: default:http:0.7.3

variables:
  code: ~
  headers: ~

request:
  path: "https://reqres.in/api/users"
  method: POST
  body[json]: {
    "name": "Morpheus",
    "job": "leader"
  }

  return:
    - .response.code: $code
    - .response.headers: $headers

expose: [$code, $headers]
