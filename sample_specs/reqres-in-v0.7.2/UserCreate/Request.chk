version: default:http:0.7.2
describe: Request

variables:
  # ::import: [Env]

  # createBody: {
  #   "name": "Morpheus",
  #   "job": "leader"
  # }

  Name: 'Morpheus'
  Job: 'leader'

request:
  path: "{$Env.server}/users"
  method: POST
  body[json]: {
      'name': $Name,
      'job': $Job,
    }
    # ::inject: $createBody

