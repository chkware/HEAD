# hello world in function
yog:
  $function: {
    name: greetings,
    params: [name],
    does: [
      $stdio.printf: {format: 'Hello %s', params: [name]},
    ],
  }
  
  $does: [
    $geetings: {name: 'hasan'},
  ]
