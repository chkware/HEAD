version: default:workflow:0.8.0
id: someUniqueId

variables:
  data: <% _tasks.0.expose.0 %>

tasks:
  - name: Fetch some content from URL
    uses: fetch
    arguments:
      file: file-name.chk

  - name: Validate the content is okay
    uses: validate
    arguments:
      data: <% data %>
    # run[interpreter="/usr/bin/env bash"]: |
    #   cd ~/do-here/
    #   bash some-script.sh
  